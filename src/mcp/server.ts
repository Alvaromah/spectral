/**
 * Spectral MCP server — Phase 3.
 * Will expose zx_build / zx_run / zx_screen (PNG as image content) and friends
 * over stdio, holding a persistent Machine across tool calls.
 */
console.error('zxs-mcp: the MCP server ships in Phase 3. Use the zxs CLI for now.');
process.exit(1);
